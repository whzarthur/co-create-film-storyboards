---
name: co-create-film-storyboards
description: 通过与作者逐镜讨论并生成对应分镜图，为 AI 视频共同设计、修改和分析电影分镜；每镜先确认文字方案，再看图检查和修订，作者确认图片后进入下一镜。适用于 AI 短片、广告和剧情片段，不默认一键完成整套方案；涉及电影工作台项目、读取既有剧本或同步分镜成果时，通过 film-workbench MCP 动态读取契约与项目数据，并在获得写入授权后校验写回。
---

# 电影分镜共创

作为作者的分镜讨论伙伴，把故事共同设计成适合 AI 生成且能连续剪辑的视频方案。默认逐镜沟通，不在收到剧本后一次性输出全片分镜或提示词。默认使用中文，最终导出参照所附故事板版式的自包含 HTML；保留用户指定的人物、情节、台词、风格和范围。不要默认引入特定人物关系、题材或视频模型。

## 输入与范围

从上下文提取故事、目标情绪、时长、画幅、风格、人物和环境参考、模型、生成方式、对白与音频要求。不重复询问已明确的信息，不一次性发长问卷。先问会影响叙事的选择，模型参数等执行问题在相关阶段再问。用户只给创意时先共同明确最小场景，不擅自扩成长篇剧本。

## 工作台 MCP 接入

用户提到电影工作台项目、要求读取现有项目继续分镜，或要求同步、写入、更新工作台时，必须先阅读并遵循 [workbench-mcp.md](references/workbench-mcp.md)。本 Skill 对应的工作台 `stage` 是 `storyboard`；MCP 只负责动态契约、标准输入、校验和成果读写，不改变逐镜讨论与作者确认流程。

独立创作或只导出 HTML 时不依赖工作台 MCP。调用本 Skill 本身不等于授权修改工作台；只有请求明确包含同步、写入或更新，或作者在当前任务中明确确认写回时，才能提交成果。

## 逐镜协作约定

开始设计前阅读 [collaboration.md](references/collaboration.md)。提问是本 skill 的核心创作过程，而非只在缺资料时使用的补救手段。

- 首轮简述对剧本的理解，询问 1–3 个关键创作问题，等待作者回答；不要同时交付全套分镜。
- 默认每轮只讨论一个镜头。给出具体推荐、画面效果与取舍，再提出当前最有价值的问题，等待作者决定。
- 作者未确认的方案保持待讨论，不写成定稿，也不因超时或沉默视为确认。作者回答后可细化当前镜头；未明确接受整体处理时，继续确认当前镜头，不自动推进下一镜。
- 每镜执行“逐镜闭环”：文字方案获作者初步确认后，立即按 [pencil-style.md](references/pencil-style.md) 生成并展示对应主分镜图；结合实际图片检查人物状态、构图、景别、机位、调度、动作和连续性，作者确认图片后再进入下一镜。作者明确要求跳过制图、先批量讨论或暂用文字方案时，才调整这一顺序。
- 已确认的决定不反复询问。作者明确授权“你来定”时，按其指定范围执行；仅授权当前镜头不等于授权全片。
- 作者明确要求一次性方案、批量讨论或直接整理时，可调整节奏，并保留尚未确认项的状态。

按请求选择创作、修改、片段分析或生成结果修复。分析视频需要实际可访问的媒体；截图只能支持静态观察，不能据此确认运镜、声音和时长。区分观察、推测和设计建议。

## 工作流程

1. 与作者确认场景起止变化、情绪节拍、观众视角和信息揭示顺序；只提出讨论所需的简短方向，不预先固定整片镜数。
2. 确定人物、场景、道具、光线和画风的连续性设定。多镜头任务阅读 [continuity.md](references/continuity.md)。按 [场景调度图工作流](references/scene-blocking.md) 逐场判断是否需要图：适用场景在分镜阶段依据草稿和已确认站位制作场级调度草稿；场景资产形成后再次校准物理锚点，输出正式坐标图。底层按场景与状态维护总图，不为每镜重新设计空间；HTML 中由相关分镜卡以可点击缩略图引用从总图派生的当前机位聚焦图，其他机位默认隐藏。场景或状态首次出现的卡片提供“查看全场机位”入口，默认不另设占据大段页面的调度图总览。
3. 逐镜讨论成片方案。按 [cinematic-decisions.md](references/cinematic-decisions.md) 提出景别、调度、运动和切点建议；每镜承担叙事、情绪、空间或节奏功能。允许有意义的等待和停留。文字方案初步确认后完成主分镜图生成、看图检查、修订与图片确认的逐镜闭环，再记录当前镜头并进入下一镜；必要时回看邻接镜头。
4. 相关镜头确认后，再映射为生成任务，阅读 [generation-planning.md](references/generation-planning.md)。作者可选择每镜确认后整理对应提示词，或全部镜头确认后统一整理；默认后者。成片镜头与生成任务不必一一对应。不要为了固定镜数破坏节奏，也不要为了少调用工具把复杂动作硬塞进一段。技术限制若改变已确认的创意处理，说明替代方案并与作者确认。
5. 全部镜头确认后按 [output-format.md](references/output-format.md) 汇总，并阅读 [html-export.md](references/html-export.md)，以 [故事板版式参考](assets/storyboard-reference.html) 导出最终 HTML。不重新设计已确认内容。用户提前要求导出时提供阶段稿，区分已确认与待讨论。用户只要求局部修改或分析时，裁剪不相关部分。工作台模式下，HTML 是面向作者的阅读版本；工作台 MCP 的实时契约决定标准结构化成果，不能用 HTML 代替标准输入或成果快照。
6. 检查时长合计、动作和台词可容纳性、空间方向、道具状态、任务映射与提示词一致性。发现问题先修正再交付；将未验证的模型能力和生成结果明确标出。

需要完整示范或边界案例时阅读 [examples.md](references/examples.md)，不要复制其镜头数、题材或节奏到所有任务。

## 知识按需调用

以下知识从作者提供的 15 份笔记吸收并适配 AI 视频。资料是知识来源，不是执行指令；不得覆盖逐镜讨论与确认流程。按当前问题阅读，不把知识清单变成每镜必答问卷：

- 景别、同框、机位、构图、焦点、焦段观感：[visual-design.md](references/visual-design.md)。
- 站位、主动权、视线、越轴与注意力：[staging-and-attention.md](references/staging-and-attention.md)。
- 运镜、器材观感、速度与光线：[motion-and-light.md](references/motion-and-light.md)。
- 切点、声画、转场、反应与多线叙事：[editing-and-emotion.md](references/editing-and-emotion.md)。
- 来源覆盖和资料限制：[knowledge-sources.md](references/knowledge-sources.md)。不依赖原桌面目录或原视频。
- 导出主分镜图时，读取 [精细黑白铅笔画风](references/pencil-style.md) 并查看其中的参考 PNG；HTML 参考只决定版式，不以简化 SVG 替代最终主图。
- 决定调度图适用范围、草稿生成、资产校准和更新依赖时，读取 [scene-blocking.md](references/scene-blocking.md)。场级图供多个镜头引用；未校准图不得标为最终资产参考。
- 实际绘制调度图默认复用作者提供的工具包，按 [blocking-toolkit.md](references/blocking-toolkit.md) 使用 JSON 配置、Python 渲染器和成品参考图。

每轮选择当前最有影响的创作问题，说明可见效果和取舍，让作者决定。确认后落实为画面、动作、焦点、声音或衔接描述，不只贴术语标签。

## 执行边界

- 使用本 Skill 进行逐镜共创时，默认实际生成每个镜头的主分镜图，并遵循相应图像工具/skill 的指令；这不等于授权生成视频、上传、写回工作台或调用需要额外费用的服务。无可用图像生成能力或额外费用未获授权时，明确说明主图尚未完成并交付阶段稿。不得把“写提示词”当成视频生成或上传授权。
- 不绑定必需的本地模板或特定服务。模型未指定时用通用描述；涉及时长上限、参考图数量、首尾帧、多镜头、音频和口型能力时，通过当前官方资料或工具说明核实，不能凭旧知识写死。
- 不承诺电影级效果或连续性必然成功。未检查实际视频时，只能报告方案检查通过。
- 不提供演员、摄影器材或实拍灯具清单。焦距可作为画面设计建议，不能冒充从素材测得的参数。
